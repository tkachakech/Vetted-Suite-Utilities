# GestureOS Requirements
# Install with: pip install -r requirements.txt

mediapipe>=0.10.0
opencv-python>=4.8.0
pyautogui>=0.9.54
numpy>=1.24.0
Pillow>=10.0.0
psutil>=5.9.0
pystray>=0.19.4

# Optional: faster MediaPipe on NVIDIA GPUs
# mediapipe-gpu  # Linux/Windows only

# System dependencies (install separately):
# Linux:
#   sudo apt install python3-tk libxcb-xinerama0 scrot xdotool
# Windows:
#   No extra dependencies (pyautogui works natively)
# macOS:
#   brew install python-tk
#   Grant Accessibility permissions in System Preferences
